{% extends ':glynn-admin:base.html.twig' %}

{% block title %}
    Picture form - {{ parent() }}
{% endblock %}

{% block body %}
    {{ form_start(form) }}

    {{ form_rest(form) }}
    <button type="submit">Save</button>
    {{ form_end(form) }}
{% endblock %}

